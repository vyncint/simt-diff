# Conformance report — reconverge 0.1.6

Generated by `simt-diff conformance`. Every row is one semantically labelled
template whose kernel was analyzed by real `cargo reconverge check --strict`.

- **predicted** is what the analyzer's *own documentation* says should happen,
  declared in the template before any run.
- **observed** is what it did, with `+Nw` counting `witness.v1` artifacts.

Environment: reconverge `0.1.6` (`46f64cc`), cuda-device pinned at
`a766fc26`, rustc `1.96.0-nightly (55e86c996)`, macOS aarch64. **No GPU
involved** — this whole report runs on a laptop.

```
```

## What held, grouped by the limitation it targets

| Documented limitation | Templates | Result |
|---|---|---|
| Interprocedural analysis is summary-based; call-site findings stay at `warning` and are never witness-promoted | `barrier_in_helper_divergent_call` | RC001 at **warning**, no witness — as documented |
| Lane-environment guards stay warnings; the interpreter cannot evaluate `lanemask_*` / `warp_id` values | `barrier_guarded_by_lanemask`, `barrier_guarded_by_warp_id` | RC001 at **warning**, no witness — as documented |
| Non-literal masks cannot be evaluated through `rustc_public`, so RC002 reports convergence rather than guessing | `mask_from_named_const`, `mask_from_active_mask` | **silent** — and correctly so: both are valid by construction |
| The unmasked convenience wrappers are outside the v1 analyzed surface | `collective_unmasked_wrapper` | **silent** — as documented |
| `shrinkmask`: RC002 v1 checks convergence, not mask arithmetic against launch shapes (0% recall) | `mask_shrunk_convergent` | **silent** — as published |
| In-scope RC001/RC002 shapes | `barrier_divergent_intra_warp`, `_nested`, `_loop_break`, `collective_under_divergence` | **confirmed + witness** in all four |
| Safe counterparts (precision) | `barrier_uniform`, `barrier_in_helper_uniform_call`, `mask_full_convergent`, plus the two valid-mask cases | **no findings** in all five |

## What this establishes, and what it does not

**Establishes.** Across fourteen independently constructed kernels, reconverge
0.1.6 did exactly what its documentation says — including the harder direction:
the three constructs it documents as warning-tier came back at warning tier
with no witness artifact, rather than being missed entirely or over-promoted.
The five safe-by-construction cases produced zero findings, which is
independent support for the "zero false positives at default confidence"
requirement, from kernels the project did not write.

**Does not establish.** This is one seed, one launch shape (`grid=1,
block=32`), one pin, one analyzer version, and fourteen hand-written
templates. It says nothing about constructs nobody thought to write. The
mutation engine and the launch matrix are what would widen it, and neither
exists yet.

**No bug was found.** That is the result, not a preamble to one.

## Refined by Stage 4

The fourteen measured rows above stand unchanged, and
[`tests/ir_seeds.rs`](../crates/simt-diff/tests/ir_seeds.rs) now holds the
mutation engine to reproducing every one of them. Two things written here read
differently in hindsight:

1. **"The mutation engine and the launch matrix are what would widen it, and
   neither exists yet."** The engine exists now, and widening the corpus to 144
   generated kernels found five places where reconverge's behaviour and its
   documentation differ — three of them because the tool is *better* than
   documented. See [`stage-4.md`](stage-4.md). The launch matrix still does not
   exist.
2. **`barrier_guarded_by_warp_id` is labelled `KNOWN_UNSAFE` above, and at
   `block=32` that is wrong.** Its own justification — "whole warps skip the
   block barrier" — needs more than one warp, and every row here was measured at
   one. The analyzer's behaviour is unaffected and the row still holds; what was
   wrong is a construction label asserted once for a property that depends on the
   launch. Stage 4 computes it per launch instead.

**"No bug was found" also stands, for this corpus.** It did not survive contact
with the generated one.
